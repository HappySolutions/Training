<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SweetsDokkana.Styles"
             NavigationPage.HasNavigationBar="false"
             x:Class="SweetsDokkana.Views.ForgotPasswordPage">
        <StackLayout>
        <Label  HorizontalOptions="FillAndExpand" 
                Text="Sweet Dokkana" 
                HorizontalTextAlignment="Center"
                VerticalTextAlignment="Center"
                Style="{StaticResource MainLabel}"/>
        <local:RoundedEntry Margin="20,0,20,0" 
                        HeightRequest="50" 
                        Placeholder="Email"
                       x:Name="mailEntry">

            </local:RoundedEntry>

            <local:RoundedButton Margin="20,40,20,0"
                        x:Name="btnSubmit"
                        HeightRequest="50"
                        Text="Submit"     
                        TextColor="#c5d86d"
                        HorizontalOptions="FillAndExpand"
                        Clicked="BtnSubmit_Clicked"/>
        <StackLayout Orientation="Horizontal">
            <Image Source="hh.jpg" HeightRequest="300" WidthRequest="300" HorizontalOptions="Center"/>
        </StackLayout>
    </StackLayout>
</ContentPage>